export enum KeyboardCode {
  ArrowUp = 'ArrowUp',
  ArrowDown = 'ArrowDown',
  ArrowLeft = 'ArrowLeft',
  ArrowRight = 'ArrowRight',
  Delete = 'Delete',
  Backspace = 'Backspace',
  Escape = 'Escape',
  Z = 'z',
  Y = 'y'
}

export enum KeyboardCommand {
  Copy = 'copy',
  Paste = 'paste'
}
