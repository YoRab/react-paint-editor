export enum KeyboardCode {
  ArrowUp = 'ArrowUp',
  ArrowDown = 'ArrowDown',
  ArrowLeft = 'ArrowLeft',
  ArrowRight = 'ArrowRight',
  Delete = 'Delete',
  Escape = 'Escape'
}

export enum KeyboardCommand {
  Copy = 'copy',
  Paste = 'paste'
}
